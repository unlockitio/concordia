# Copyright (c) 2026 Unlockit. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Tier 2 (Auctions) — auction-specific shared data: AuctionId and the
# reserved metadata/context key registry. The generic shapes (Metadata,
# AnyValue, ChoiceContext, ExtraArgs) live in cap-core-metadata; every
# auction module imports them from there.
sdk-version: 3.4.11
name: cap-auctions-types-v1
version: 0.1.0
source: daml
dependencies:
- daml-prim
- daml-stdlib
build-options:
  - --target=2.1
