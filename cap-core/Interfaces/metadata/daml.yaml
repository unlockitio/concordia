# Copyright (c) 2026 Unlockit. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Tier 1 — the value types every cap-core interface carries: `Metadata`,
# `AnyValue`, `ChoiceContext`, `ExtraArgs`. Mirrors the Token Standard's
# `Splice.Api.Token.MetadataV1` shapes without importing it.
sdk-version: 3.4.11
name: cap-core-metadata-v1
version: 0.1.0
source: daml
dependencies:
- daml-prim
- daml-stdlib
build-options:
  - --target=2.1
